import React, { Fragment } from 'react';
import { Link } from 'react-router-dom';

const Home = () => (
  <Fragment>
    <h1>xD</h1>
    <Link to="/login">Sign In</Link>
  </Fragment>
);

export default Home;
